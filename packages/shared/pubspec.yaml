name: fuelrush_shared
description: Shared types, constants, validators, and models for FUELRUSH.
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  freezed_annotation: ^3.1.0
  json_annotation: ^4.12.0
  equatable: ^2.1.0
  intl: ^0.20.2

dev_dependencies:
  build_runner: ^2.15.0
  freezed: ^3.0.0
  json_serializable: ^6.12.0
  test: ^1.26.3